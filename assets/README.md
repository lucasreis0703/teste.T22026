# Assets for Streamlit app

Place a logo image in this folder and name it `logo.png`.

The Streamlit app will automatically display that image in the sidebar.

Example:
- `assets/logo.png` (recommended)

If you do not provide a `logo.png`, the app will show a simple text placeholder instead.
